# wit/deps.toml
# Dependency manifest for scryer-prolog CLI component

# Reference the library component's WIT interface
scryer-prolog = { path = "../../wit" }

# WASI CLI dependency for command world
wasi-cli = "0.2.0"
